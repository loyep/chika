{
  "name": "@chika/prisma",
  "version": "0.1.0",
  "license": "MIT",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "db-generate": "yarn prisma generate",
    "db-push": "yarn prisma db push",
    "dev2": "yarn prisma studio --port 5556"
  },
  "dependencies": {
    "@prisma/client": "^4.7.1"
  },
  "devDependencies": {
    "dotenv-cli": "^7.0.0",
    "prisma": "^4.7.1",
    "typescript": "^4.9.4"
  }
}
